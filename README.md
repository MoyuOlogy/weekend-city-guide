# 🧭 周末探索 · 周末城市探索指南

一个面向大学生的周末城市探索指南：根据 **偏好 · 天气 · 预算 · 同行人数** 推荐本地活动，支持 **组队出发、打卡记录、攻略分享**，并能在 **多台设备之间同步**。

🔗 在线访问：**https://moyuology.github.io/weekend-city-guide/**

---

## ✨ 功能（v2.0）

### 🔍 推荐引擎（可解释加权评分）
- 城市（8 城）、周六/周日、人均预算、同行人数、可支配时长、10 类活动多选、额外偏好（近 / 省 / 出片 / 新手友好）
- 每条推荐都给出 **匹配度 %** 和 **推荐理由 / 风险提示**（如「命中偏好 · 桌游」「¥45 在预算内」「超预算 ¥30」「至少 2 人」），而不是黑箱排序

### 🌤 天气联动
- 接 **Open-Meteo** 真实预报（免费无 Key），按「即将到来的那个周六/周日」精确取当天数据（`forecast_days=7`）
- 下雨自动压低户外活动权重、优先室内；断网自动降级为示例天气并明确标注

### 👥 组队 / 📍 打卡 / 📝 攻略
- 组队：建队、加入退出、满员进度环、一键复制邀请文案、发起人可解散
- 打卡：地点 / 日期 / 花费 / 时长 / 心情 / 感想，自动统计次数·总时长·总花费·去过的地点数
- 攻略：发布、标签、点赞
- 还能 **自建活动** 加入活动库，房间内共享

### 🌐 跨设备同步（v2 新增，重点）
三种方式，互为兜底：

| 方式 | 机制 | 适用 |
| --- | --- | --- |
| **云端房间** | 6 位房间码 → `textdb.dev` 上的一个 JSON 文档；页面每 6 秒轮询合并，改动后 0.6 秒自动上传 | 手机建队，电脑/同学立刻看到 |
| **分享链接** | 把数据打包进网址 `#r=...`，任何设备打开即导入 | 一次性发给同学，永久有效、不依赖第三方服务 |
| **导入 / 导出 JSON** | 本地文件备份 | 换机迁移、留档 |

同步不是简单的「整份覆盖」，而是：

- **发布前先拉取合并**（read-modify-write），避免后写的客户端用较少的数据抹掉别人的贡献
- 按 `id` 求并集 + 同 `id` 取 `ts` 较新者；**墓碑机制**保证删除不会被旧快照复活
- 点赞按「每人最后一次操作生效」（`{昵称: ±时间戳}`），既不会把取消赞刷回来，多人点赞也能正确累加
- 失败自动降级：网络不通时数据只存本机，界面明确显示 🔴 同步失败，不会丢数据

### 🎨 界面（按小红书真实设计规范重做）

配色、图标、卡片结构、栅格参数不是"照着感觉调"的，而是从参考页面 `xiaohongshu.txt`（小红书真实 DOM）里提取的：

| 项 | 取值来源 |
| --- | --- |
| 品牌主色 | `#ff2442`（页面内联样式实测值） |
| 文字层级 | 正文 `#333` / 次要 `#666` / 弱化 `#999`，分隔线 `#eee` |
| 图标 | 24×24 单色矢量，**不用 emoji**；其中 7 个直接取自其 SVG symbol（`home` / `search` / `addM` / `creator` / `like` / `liked` / `menu` / `arrowRight`），其余按同一网格自绘 |
| 卡片结构 | 对齐其 `.note-item` 的真实层级：`.cover` → `.footer` → `.title` → `.author-wrapper`（`.avatar-container` + `.name` | `.like-wrapper` + `.count`） |
| 栅格 | 1440 视口下 5 列、卡片宽 215.2px、列间距 32px（由其 `translate()` 步长 247.2px 反推） |

- **桌面端**：固定左侧导航栏（首页 / 组队 / 打卡 / 攻略 / 我的 + 发布 / 同步 / 主题）+ 居中圆角搜索框 + 纯文字分类 Tab（红色下划线指示）+ 最多 5 列瀑布流
- **移动端**：底部 Tab 栏（中央红色方块「＋」）+ 顶部搜索栏 + 可横滑分类 + 2 列瀑布流；`env(safe-area-inset-bottom)` 适配全面屏
- 卡片只保留小红书卡片该有的四个元素：**封面（含价格/匹配度角标）、标题、作者行、点赞** —— 标签、描述、指标都收进详情弹层
- 交互：点封面出详情、**双击封面点赞**（爱心爆破）、点赞心跳、滚动入场动画、骨架屏、触感反馈、分类实时筛选与搜索
- **配色禁用蓝紫**：全站仅用红 / 橙 / 黄 / 橄榄绿 + 暖中性色（浅色与深色两套主题，默认浅色）。这项有自动化色相断言守着

---

## 🛠 技术栈

单文件 `index.html`，原生 HTML + CSS + JavaScript，**零依赖、零构建**，GitHub Pages 静态托管。数据存 `localStorage`（v1 数据自动迁移）。

---

## ✅ 质量验证

不是「看起来能跑」，而是用 Edge 无头浏览器**真实加载页面、真实发网络请求**跑出来的：

| 测试 | 覆盖 | 结果 |
| --- | --- | --- |
| 颜色审计（Node） | 扫描全站颜色，断言不存在 hue ∈ [185°, 335°] 的蓝/紫色相 | ✅ 通过（仅 8°–115° 与 350°） |
| 行为测试（浏览器内） | 渲染 / 筛选 / 搜索 / 点赞 / 双击点赞 / 详情弹层 / 发布四种内容 / 合并与墓碑 / 分享链接往返 / XSS 转义 / 持久化 | ✅ **47/47** |
| 布局与移动端（iframe 精确视口） | 375px 与 1280px 下侧栏/底栏切换、列数、横向溢出、Tab 是否全在视口内、卡片是否依赖动画才可见 | ✅ **24/24** |
| 跨设备同步（真网络） | 建房 → 发布 → 清空本地 → 从云端拉回；中文长文本；空房间；断网降级；轮询生命周期 | ✅ **17/17** |

复现方式（需 Node.js + Edge）：

```powershell
powershell -File tests\run-tests.ps1
```

开发过程中被这套测试抓出并修掉的真实缺陷：

1. `forecast_days=3` 导致「本周六」显示的是周三天气
2. 降水判定口径不一致，出现「降水概率 39%」却提示「☔ 有降水」
3. `toISOString` 在 UTC+8 晚间会跨天
4. **发布整份覆盖会抹掉同伴数据** → 改为读-改-写
5. **`Accept: application/json` 让 textdb 把响应再包一层 JSON 字符串**，导致合并静默失效（只有真实端到端测试才能发现）
6. ntfy.sh 免费版每日消息配额（实测触发 HTTP 429）→ 换成 textdb.dev
7. **分类条放在矮的静态 header 里导致永远吸不住顶** —— `position:sticky` 被限制在父容器范围内，必须与搜索栏同处一个 sticky 容器
8. **卡片可见性绑在动画上**（`opacity:0` 等 JS 加类才显形）→ 动画没跑整页就是空白。改为「默认可见 + 动画只做增强」，并支持 `prefers-reduced-motion`

### 测试踩坑（值得记下来）

- **Windows 无头窗口有最小宽度**：`--window-size=390` 时实际布局视口是 **503px**，截图只是把 503 裁成 390 —— 一度让我误判"右列被挤出屏幕"。改用 iframe 强制精确视口后，量到 `body.scrollWidth == clientWidth`，确认根本没有横向溢出。**结论：先用数值诊断（`scrollWidth` / `getBoundingClientRect`）确认，再动手改样式。**
- **无头虚拟时钟不驱动 CSS 动画**：动画冻结在第 0 帧，`animation ... both` 的卡片会一直 `opacity:0`，截图看着像"没渲染"。用 `--force-prefers-reduced-motion` 触发无障碍降级分支即可正常截图——顺带把 `prefers-reduced-motion` 这条路径也验证了。

---

## ⚠️ 已知限制

1. **房间基于公共服务 textdb.dev**：房间码即钥匙，任何拿到它的人都能读写，**请勿放敏感信息**；公共服务的保留策略与可用性不由本项目保证——重要内容请用「分享链接」或导出 JSON 留档
2. **活动是示例数据**（16 条内置），未接真实活动源；封面是用 CSS 渐变 + emoji 生成的占位图，没有真实照片
3. 距离为示例值，未接定位与真实通勤时间
4. 无账号体系：昵称仅在本机登记，换设备需重新填
5. 同步靠轮询（6 秒），不是 WebSocket 实时推送
6. **图标版权说明**：为对齐参考设计，部分图标路径取自小红书页面的 SVG symbol 定义，仅供本项目学习/原型用途；若要商用请替换为原创图标

## 🗺 后续可迭代

- [ ] 接真实活动数据源（大麦 / 豆瓣同城 / 城市文旅）
- [ ] 定位 + 真实距离与通勤时间
- [ ] 后端 + 登录（Supabase / Cloudflare Worker）替代公共存储，支持权限与长期保存
- [ ] 生成分享海报、一键导出周末计划到日历
- [ ] 多人投票决定去哪玩

## 🚀 本地预览

```bash
# 直接双击 index.html 即可；或起静态服务
python -m http.server 8080
```
